/// Defines the possible states of a Kit button.
enum KitButtonState {
  /// The button is active and can be interacted with.
  enabled,

  /// The button is visually greyed out and cannot be interacted with.
  disabled,

  /// The button shows a loading indicator and cannot be interacted with.
  loading,
}
