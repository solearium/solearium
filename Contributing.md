# 🧭 Contributing Guidelines

Thank you for contributing! Please follow these steps carefully.


## 🚀 Getting Started

1. Fork the repository.


2. Clone your fork locally.

```bash 
git clone https://github.com/solearium/<repo-name>.git
```


3. Create a new branch for your work:

```bash
git checkout -b <branch-name>
```

- Examples:

  `fix/navbar-overlap`

  `feature/dark-mode`

  `docs/update-readme`

---

### 🧠 Working on Issues

1. Find an issue labeled `🕳️ Unassigned` in the Issues tab.


2. Assign it to yourself by commenting: ``/assign``


3. Once assigned, work on your fix or feature in your local branch.


4. When you’re done, push your branch and open a Pull Request.




---

### ✅ Opening a Pull Request (PR)

When you open a PR, follow these rules to make sure it’s accepted:

**🧩 For Issue-Based Work**

- Link your PR to the issue you worked on.

- Example PR body :

  `Fixes #42`

- Only the person assigned to the issue should open the PR.
  

- Example PR Format (i.e For Issue No. 52)

  Title: `Fix: correct footer link color`
  Body: `Fixes #52`



**🌟 For New Features**

- If you’re adding a new feature not linked to an issue:

- Include one of these keywords in your PR title:
  `feature, feat, enhancement, or improvement`

- Example PR title:

  `Feature: add dark mode toggle`




---

### 🧩 Commands You Can Use in Comments

``Command``	Description

- ``/assign``	Assign yourself to an issue
- ``/unassign``	Unassign yourself from an issue
- ``/done``	Mark the issue as completed (after your PR is merged)


### 🧹 Good Practices

-  Keep commits clear and atomic:

```bash
git commit -m "Fix: update footer alignment"
```

- Make sure your code passes all required checks before opening a PR.

- Write meaningful PR titles

- Respond to feedback or requested changes promptly.



---

## 💡 Example Workflow

### 1. Find an open issue
### 2. Assign yourself
``/assign``

### 3. Create a branch
```bash
git checkout -b fix/navbar-spacing
```

### 4. Work on your code and commit
```bash
git add .
git commit -m "Fix: adjust navbar spacing for mobile view"
```

### 5. Push your branch
```bash
git push origin fix/navbar-spacing
```

### 6. Open a PR with:
 Title: `Fix: adjust navbar spacing`
 Body: `Fixes #42`


---

## 💬 Questions?

- If anything is unclear:
Ask in your team chat with the issue or PR link.


---
